# 模块 0　导论：SRE 视角下的变更与交付

> 对应《面向 SRE 的 CI/CD 课程目录》模块 0。
> 目标：先回答「为什么」——为什么这套 CI/CD 要以 SRE 的视角来学，交付链路长什么样，以及我们要搭一套什么样的环境。

---

## 0.1 为什么 SRE 要关心 CI/CD

### 0.1.1 先说清楚：SRE 到底在管什么

SRE（Site Reliability Engineering，站点可靠性工程）是 Google 提出的一套工程方法论，一句话概括：

> **用软件工程的手段，去解决运维领域的可靠性问题。**

传统运维和 SRE 的一个关键区别在于「怎么看变更」：

| | 传统运维视角 | SRE 视角 |
|---|---|---|
| 变更 | 尽量避免，越少越好 | 变更是常态，关键是怎么「可控地」变 |
| 稳定性 | 靠「不动」来维持 | 靠「快速、可回滚、可观测的交付」来维持 |
| 故障 | 出了事再救火 | 用错误预算量化风险，把可靠性做成可度量的指标 |

所以 SRE 关心的不是「要不要发布」，而是「发布能不能又快又稳、出了事能不能快速回滚」。而这套能力，正好就是 CI/CD 要解决的核心问题。

### 0.1.2 变更即风险

这是 SRE 最重要的一个判断：**生产环境的大部分事故，都与某次变更有关。**

- 上线了新代码 → 触发 bug
- 改了配置 → 配置错误
- 扩缩容、网络策略调整 → 引发连锁反应

既然「变更是风险的主要来源」，那么结论就不是「别变更」，而是：

1. **让变更本身更可靠**——把人工操作变成自动化流水线，减少手抖；
2. **让变更可回滚**——任何变更都要能一键退回去，降低事故的「爆炸半径」；
3. **让变更可观测**——发布前、中、后都有指标兜底，能第一时间发现问题；
4. **小步快跑**——把大变更拆成小变更，每次只动一点点。

这四点，正是后面模块 2～7 会逐条落地的能力。模块 0 先把「为什么」讲透。

### 0.1.3 发布与 SLI / SLO / 错误预算

SRE 衡量「服务健不健康」，用一套指标语言：

- **SLI（Service Level Indicator，服务水平指标）**：具体可量化的指标，比如
  - 可用性 = 成功请求数 / 总请求数
  - 延迟（P99 延迟 < 200ms）
  - 错误率 < 0.1%
- **SLO（Service Level Objective，服务水平目标）**：对 SLI 设定的目标值，比如「月度可用性 ≥ 99.9%」。
- **SLA（Service Level Agreement，服务水平协议）**：对客户的法律/商业承诺，通常比 SLO 更宽松、违约有赔偿。

> 记住三者的关系：**SLI 是尺子，SLO 是目标，SLA 是合同。** 团队内部运维看 SLO，对外承诺才用 SLA。

**错误预算（Error Budget）**：`错误预算 = 1 - SLO`。

- SLO 是 99.9% 可用 → 每月允许有 0.1%（约 43 分钟）的不可用时间，这就是错误预算。
- **发布会消耗错误预算**：每次上线都有概率出错，出错就占用这段「允许失败」的额度。
- 错误预算的用法：
  - 预算充足 → 可以大胆发，加快迭代；
  - 预算快耗尽 → 冻结发布，先把可靠性补回来。

这就是 SRE 平衡「可靠性 vs 交付速度」的量化工具。CI/CD 里加灰度、自动回滚、发布门禁，本质都是在**保护和合理使用错误预算**。

### 0.1.4 MTTR 与变更失败率

两个衡量「出事之后能不能快速恢复」的关键指标：

- **MTTR（Mean Time To Repair，平均修复时间）**：从故障发生到恢复所用的平均时间。
  - 事故不可避免，SRE 追求的是**缩短 MTTR**，而不是「零事故」。
  - CI/CD 如何缩短 MTTR？——**快速回滚**。发布后发现问题，一键退回上一版本，比「定位 bug → 改代码 → 重新上线」快几个数量级。
- **变更失败率（Change Failure Rate）**：导致需要回滚、热修复、紧急补丁的变更占总变更的比例。
  - 这是 DORA 四指标之一（0.2 会展开）。
  - 变更失败率高的团队，说明「变更质量」差——测试没兜住、门禁形同虚设、发布策略太激进。

### 0.1.5 本课程要交付的东西

学完这套课程，你要能回答并落地这三个问题：

1. 一次变更，怎么从「人肉敲命令」变成「可重复、可审计的流水线」？（模块 2～4）
2. 一次发布，怎么做到「小步、灰度、可自动回滚」？（模块 5～6）
3. 整条链路，怎么做到「安全、合规、可观测」？（模块 7）

> 本模块无动手实验，但请带着下面这个问题读后面的内容：
> **「我们团队现在一次发布，从提交代码到上线，中间有哪些环节是人在手动操作？哪些环节出了事无法快速回退？」**

---

## 0.2 交付链路全景

### 0.2.1 从 commit 到生产的价值流

先建立全局视角。一次软件交付，代码要经过的典型路径（价值流）：

```
写代码 → 提交(commit) → 构建(build) → 测试(test) → 制品(artifact)
      → 部署(deploy) → 发布(release) → 监控(observe)
```

拆开看每一段的职责，以及它对应的课程模块：

| 阶段 | 在做什么 | 常见工具 | 对应模块 |
|---|---|---|---|
| 提交 | 代码进入版本库，触发流水线 | Git + GitLab | 模块 1 |
| 构建 | 编译、打成镜像/制品 | Kaniko、BuildKit | 模块 2 |
| 制品 | 统一存放、扫描、签名 | Harbor | 模块 2 |
| 测试 | 单测、集成、静态检查、安全扫描 | 各类 test/lint 工具 | 模块 3 |
| 部署 | 把制品装到目标环境 | Argo CD（GitOps） | 模块 4 |
| 发布 | 控制流量切换、灰度、回滚 | Istio + Flagger | 模块 5 |
| 监控 | 发布前后的指标/日志/追踪 | Prometheus + Istio 可观测 | 模块 6 |

关键认知：**「部署（deploy）」和「发布（release）」不是一回事。**

- 部署：把新版本的服务进程/容器真正跑起来（新版本已经在环境里，但还没接流量）。
- 发布：把用户流量切到新版本（真正对用户可见）。

这个区分是模块 5（蓝绿、金丝雀、渐进式交付）的基础——蓝绿/金丝雀的本质，就是「先部署、再通过流量切换来发布」。

### 0.2.2 交付效率四指标（DORA）

DORA（DevOps Research and Assessment）调研多年，总结出衡量软件交付能力的四个核心指标，业界基本公认：

| 指标 | 定义 | 衡量的是什么 |
|---|---|---|
| **部署频率** | 成功发布到生产的频率 | 交付速度 |
| **变更前置时间** | 从代码提交到成功上线的耗时 | 交付速度 |
| **变更失败率** | 变更导致需回滚/热修的比例 | 交付质量 |
| **服务恢复时间** | 故障后恢复服务耗时（即 MTTR） | 恢复能力 |

**优秀团队 vs 落后团队的典型区间**（供对标，非硬标准）：

| 指标 | 高性能团队 | 低性能团队 |
|---|---|---|
| 部署频率 | 每天多次 | 每周/每月一次 |
| 变更前置时间 | 小时级 | 周/月级 |
| 变更失败率 | 0～15% | 45～60% |
| 服务恢复时间 | 小时级内 | 周级 |

> 四个指标是一套相互关联的体系：**要想「部署频率高」还不出事，就必须「前置时间短、失败率低、恢复快」**。单独追求一个指标（比如拼命提速）往往以牺牲质量为代价。这就是后面模块要系统化建设流水线的原因。

### 0.2.3 价值流图怎么画

价值流图（Value Stream Mapping）是把 0.2.1 那条链路「照进现实」的工具——画出你们团队现在真实是怎么交付的，找出瓶颈。

画法（从简单开始）：

1. **列出所有步骤**：从「开发提交代码」到「上线完成」的每一个环节，不管是不是自动化。
2. **标注每步的状态**：
   - 是人工还是自动？
   - 大约耗时多久？
   - 有没有「等待时间」（比如等 review、等审批、等环境）？
3. **标注痛点**：哪里经常出错、哪里最慢、哪里回滚困难。
4. **计算两个时间**：
   - **实际工作时间（处理时间）**：真正在「干活」的时间。
   - **总流转时间（前置时间）**：从提交到上线的端到端耗时。
   - 两者的差，就是「等待/排队时间」——通常这是最大的浪费来源。

一个典型「人肉发布」团队的价值流，往往是：写代码 2 天 → 等 review 1 天 → 手动构建 1 小时 → 手动部署 2 小时 → 上线。**瓶颈几乎都在「等待」和「人工操作」上，而不是「写代码慢」。**

### 0.2.4 实验：绘制团队现状价值流图

**目标**：产出你们团队当前交付链路的一张真实价值流图，作为后续所有模块改造的「基准线（baseline）」。

**步骤**：

1. 召集相关同学（开发、测试、运维），白板/纸上列出从「提交代码」到「上线」的全部步骤。
2. 对每一步标注：人工/自动、耗时、等待时间、出错频率、能否回滚。
3. 标出「最痛」的三个环节（最慢 / 最易错 / 最难回滚）。
4. 画一张简化流程图，并写下当前团队的 DORA 四指标估计值（能拿到真实数据最好）。

**产出**（建议沉淀成文档，后面每个模块都能回来对照）：

- 价值流图一张
- 三个最痛环节清单
- 当前 DORA 四指标的基线估计
- 一句话目标：**「这次课程结束后，我希望把哪个环节的哪个指标改到什么程度？」**

> 这张图不是交作业用的，而是后面每个模块的「改造对象」。学完一个模块就回来更新一次这张图，看瓶颈有没有被解决。

---

## 0.3 课程技术栈与实验环境

### 0.3.1 技术栈选型说明

本课程围绕一条主链路选型，所有模块复用同一套环境：

| 环节 | 选型 | 为什么选它 |
|---|---|---|
| 代码托管 + CI | GitLab / GitLab CI | 代码与流水线一体，MR 门禁、受保护分支开箱即用 |
| 镜像构建 | Kaniko | 无特权构建，避免 DinD 的安全与性能坑 |
| 制品仓库 | Harbor | 镜像治理、漏洞扫描、签名、代理缓存 |
| CD / GitOps | Argo CD | 声明式、差异可视化、自动同步 |
| 服务网格 / 流量治理 | Istio | 蓝绿/金丝雀的流量切换能力 |
| 渐进式交付 | Flagger | 指标驱动的自动放量与回滚（模块 5） |
| 本地集群 | Kind（备选 Minikube） | 轻量、快速，适合本地实验 |

> 为什么是 Kind：多节点、启动快、资源占用小，适合在一台开发机上跑整套实验。Minikube 作为备选，操作大同小异。

### 0.3.2 前置环境准备

请先确认本机具备以下工具（版本用较新稳定版即可）：

| 工具 | 用途 | 校验命令 |
|---|---|---|
| Docker / 容器运行时 | 跑本地容器 | `docker version` |
| kubectl | 操作 K8s | `kubectl version --client` |
| Helm | 安装应用 | `helm version` |
| Kind | 本地 K8s 集群 | `kind version` |
| istioctl | 安装/管理 Istio | `istioctl version` |

各工具安装方式略（官网文档为准）。下面假设以上工具都已就绪。

### 0.3.3 搭建统一实验集群

**第 1 步：创建多节点 Kind 集群**

Istio 需要多节点（至少一个 control-plane + 若干 worker）。新建配置文件：

```yaml
# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
```

创建集群：

```bash
kind create cluster --name sre-cicd --config kind-config.yaml
kubectl cluster-info --context kind-sre-cicd
```

**第 2 步：安装 Istio（含默认 sidecar 注入）**

```bash
# 下载 Istio（以官方最新稳定版为准）
curl -L https://istio.io/downloadIstio | sh -
cd istio-*
export PATH=$PWD/bin:$PATH

# 以 demo profile 安装（开箱即用的最小集，适合实验）
istioctl install --set profile=demo -y

# 验证 istiod 已就绪
kubectl get pods -n istio-system
```

**第 3 步：开启默认命名空间的 sidecar 自动注入**

```bash
kubectl label namespace default istio-injection=enabled
# 查看是否生效
kubectl get namespace -L istio-injection
```

> sidecar 注入是模块 5（Istio 流量治理）和模块 6（网格可观测）的前提，这里先把它打开，后续部署的应用会自动带上 Envoy sidecar。

### 0.3.4 验证环境

部署一个最小的示例应用，确认集群 + Istio 都正常。以 bookinfo 为例（Istio 官方示例）：

```bash
# 开启注入后，bookinfo 会自动带上 sidecar
kubectl apply -f samples/bookinfo/platform/kube/bookinfo.yaml

# 等待 Pod 就绪，确认每个 Pod 是 2/2（应用容器 + envoy sidecar）
kubectl get pods
```

看到 `2/2 Running` 即说明：应用起来了、Envoy sidecar 也注入成功了。

**清理（学完本模块后可选）**：

```bash
kind delete cluster --name sre-cicd
```

### 0.3.5 实验：搭建统一实验集群

**目标**：本模块唯一的动手实验——把整条课程要用的集群跑起来，作为后续所有模块的底座。

**验收标准**：

1. `kind get clusters` 能看到 `sre-cicd`；
2. `kubectl get pods -n istio-system` 里 istiod 状态为 `Running`；
3. `default` 命名空间已开启 `istio-injection`；
4. 部署一个示例应用后，Pod 为 `2/2`（应用 + sidecar）。

**常见坑**：

- **Kind 节点资源不足**：Istio 组件较多，若 Pod 一直 Pending，检查本机 Docker 分配的内存/CPU，建议 ≥ 4G 内存。
- **`istioctl` 不在 PATH**：记得 `export PATH=$PWD/bin:$PATH`，或把 `bin/istioctl` 拷到 `/usr/local/bin`。
- **sidecar 没注入**：确认 `kubectl label namespace default istio-injection=enabled` 已执行，且部署时命名空间标签正确。

---

## 本模块小结

- **为什么**：SRE 靠「可控、可回滚、可观测的变更」来维护可靠性，而不是靠「不变更」；错误预算是平衡可靠性与速度的量化工具。
- **全景**：一次交付从 commit 到生产是一条价值流；DORA 四指标（部署频率、前置时间、失败率、恢复时间）是衡量这条链路好坏的标尺；价值流图是找到瓶颈的起点。
- **环境**：GitLab + Kaniko + Harbor + Argo CD + Istio + Flagger + Kind，一套环境贯穿全课程。

**进入模块 1 前，请完成**：

1. 画出你们团队的现状价值流图，并写下 DORA 四指标基线；
2. 把 Kind + Istio 集群跑起来并验证 sidecar 注入成功。
